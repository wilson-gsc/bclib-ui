import { Status } from "../_helpers/enums/status";

export class Publisher {
    id?: string;
    name?: string;
    status?: Status;
    created_at?: Date;
    updated_at?: Date;
    created_by?: Date;
}