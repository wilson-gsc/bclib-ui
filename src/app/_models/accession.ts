import { Status } from "../_helpers/enums/status";

export class Accession {
    id?: string;
    name?: string;
    code?: string;
    status?: Status;
    created_at?: Date;
    updated_at?: Date;
    created_by?: Date;
}