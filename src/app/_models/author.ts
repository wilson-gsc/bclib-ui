import { Status } from "../_helpers/enums/status";

export class Author {
    id?: string;
    full_name?: string;
    status?: Status;
    number?: number;
    created_at?: Date;
    updated_at?: Date;
    created_by?: Date;
}